---
const tabs = ['Home', 'Clips', 'Books'];
---

<nav id="main-nav" class="fixed top-0 left-0 right-0 z-50 transition-all duration-300">
  <div class="nav-inner flex items-center justify-between px-6 md:px-12 py-5">
    <a href="/" class="nav-logo flex items-baseline gap-2" data-tab-link="home">
      <span class="font-display text-2xl tracking-widest text-accent">THOUGHTS EVOKED</span>
      <span class="font-mono text-[10px] text-text-3 tracking-widest">/MEDIA</span>
    </a>

    <div class="tab-group flex gap-1">
      {tabs.map((tab) => (
        <button
          class:list={[
            'tab-btn font-mono text-xs tracking-widest uppercase px-4 md:px-5 py-2.5 border border-transparent text-text-2 rounded-full transition-all duration-200',
            tab === 'Home' ? 'active' : '',
          ]}
          data-tab={tab.toLowerCase()}
        >
          {tab}
        </button>
      ))}
    </div>
  </div>
</nav>

<script>
  function initNav() {
    const nav = document.getElementById('main-nav');
    if (!nav) return;

    window.addEventListener('scroll', () => {
      if (window.scrollY > 60) {
        nav.style.background = 'rgba(7, 7, 7, 0.92)';
        nav.style.borderBottom = '1px solid var(--border)';
        nav.style.backdropFilter = 'blur(20px)';
      } else {
        nav.style.background = 'transparent';
        nav.style.borderBottom = '1px solid transparent';
        nav.style.backdropFilter = 'none';
      }
    }, { passive: true });

    function switchTab(tabId: string) {
      const gsap = (window as any).__gsap;
      if (!gsap) return;

      const currentPage = document.querySelector('.page.active');
      const nextPage = document.getElementById(`page-${tabId}`);

      document.querySelectorAll('.tab-btn').forEach(btn => btn.classList.remove('active'));
      document.querySelector(`[data-tab="${tabId}"]`)?.classList.add('active');

      if (!nextPage || currentPage === nextPage) return;

      gsap.to(currentPage, {
        opacity: 0,
        y: -12,
        duration: 0.25,
        ease: 'power2.in',
        onComplete: () => {
          currentPage?.classList.remove('active');
          nextPage.classList.add('active');
          gsap.fromTo(nextPage,
            { opacity: 0, y: 16 },
            { opacity: 1, y: 0, duration: 0.4, ease: 'power3.out' }
          );
          (window as any).__lenis?.scrollTo(0, { immediate: true });
          (window as any).__ScrollTrigger?.refresh();
        }
      });
    }

    document.querySelectorAll('.tab-btn').forEach(btn => {
      btn.addEventListener('click', () => {
        const tabId = btn.getAttribute('data-tab');
        if (tabId) switchTab(tabId);
      });
    });

    const logo = document.querySelector('.nav-logo');
    if (logo) {
      logo.addEventListener('click', (e) => {
        e.preventDefault();
        switchTab('home');
      });
    }
  }

  document.addEventListener('DOMContentLoaded', initNav);
</script>
